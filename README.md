This is my GUI training project with CustomTkinter.
I hope you enjoy it.


### Requirements

* Python 3.9+
* Install tkinter:
  * `pip install tkinter`
* Install customtkinter:
  * `pip install customtkinter`
* Install pyttsx3:
  * `pip install pyttsx3==2.90`
